<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TARpe24_Mobiilirakendused.MemoGame.MemoGame"
             Title="MemoGame">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15" HorizontalOptions="Center">
            
            <Label Text="Mälumäng (Memo)" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center"/>

            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10">
                <Label Text="Vali teema:" VerticalOptions="Center" FontSize="16"/>
                <Picker x:Name="ThemePicker" Title="Vali stiil" WidthRequest="150" SelectedIndexChanged="OnThemeChanged">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>Hele</x:String>
                            <x:String>Tume</x:String>
                            <x:String>Värviline</x:String>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
            </HorizontalStackLayout>

            <Label x:Name="StatusLabel" Text="Käigud: 0 | Paarid: 0/8" FontSize="18" HorizontalOptions="Center" />
            <Label x:Name="BestScoreLabel" Text="Parim tulemus: -" FontSize="14" HorizontalOptions="Center" TextColor="Gray"/>

            <Grid x:Name="GameGrid" 
                  ColumnSpacing="10" 
                  RowSpacing="10" 
                  HorizontalOptions="Center" 
                  VerticalOptions="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
            </Grid>

            <Button Text="Uus Mäng" Clicked="OnNewGameClicked" WidthRequest="200" Margin="0,15,0,0"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>